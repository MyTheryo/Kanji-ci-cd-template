# App Store Connect API authentication (preferred over Apple ID)
# Configure these via environment variables

app_identifier ENV["BUNDLE_ID"] || "com.example.app"
apple_id ENV["APPLE_ID"]
team_id ENV["TEAM_ID"]
itc_team_id ENV["ITC_TEAM_ID"] || ENV["TEAM_ID"]
